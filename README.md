# Intelligent Shoplifting Detection System

AI-powered surveillance platform using CNN-LSTM (MobileNetV2 + LSTM) to detect shoplifting from CCTV videos.

## Setup Instructions

1. Clone the repository and place your trained model as `models/best_model.keras`
2. Install dependencies:
   ```bash
   pip install -r requirements.txt